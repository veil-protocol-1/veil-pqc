# Required in production — generate with: openssl rand -base64 32
JWT_SECRET=change-me-in-production

# Comma-separated allowlisted wallet addresses (Safe is hardcoded)
# ALLOWLIST=0xAddress1,0xAddress2
